name: modular_cart_core
description: A headless Dart cart engine for configurable products with option/modifier support and deterministic cart line identity (no duplicates, no accidental merges).
version: 1.0.0
homepage: https://github.com/winnrz/modular_cart_core
repository: https://github.com/winnrz/modular_cart_core.git

environment:
  sdk: ^3.10.4

# Add regular dependencies here.
dependencies:
  equatable: ^2.0.8
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
